# ruff 의 기본 규칙 집합은 버전마다 달라진다. 0.16 에서 넓어지면서, 설정 없이
# 돌던 CI 가 코드를 하나도 안 고쳤는데 빨간불이 됐다(로컬 0.15 는 통과,
# CI 0.16.5 는 BLE001/RUF059/FURB136 로 실패 — 2026-08-29).
#
# 그래서 규칙을 여기에 적어 둔다. 이러면 러너가 어떤 버전을 받아도 같은 걸
# 본다. 값은 ruff 의 오랜 기본값이고, 이 코드가 그 기준으로 쓰였다.
#   E4/E7/E9 pycodestyle 오류, F pyflakes
# 규칙을 넓히는 건 그 자체로 하나의 작업이다 - 지금 코드에 맞춰 함께 고쳐야 한다.
[lint]
select = ["E4", "E7", "E9", "F"]
